spec starcoin_framework::ring {
    spec create_with_capacity {
        pragma intrinsic = true;
    }


    spec is_full {
        pragma intrinsic = true;
    }

    spec capacity {}

    spec push {
        pragma intrinsic = true;
    }


    spec borrow {
        pragma intrinsic = true;
    }

    spec borrow_mut {}

    spec index_of {
        pragma intrinsic = true;
    }

    spec destroy {
        pragma intrinsic = true;
    }
}
