name: Cancel
on: [push]
jobs:
  cancel:
    name: 'Cancel Previous Runs'
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - uses: styfle/cancel-workflow-action@0.3.1
        with:
          workflow_id: 511061,1098916
          access_token: ${{ secrets.GH_ACCESS_TOKEN }}